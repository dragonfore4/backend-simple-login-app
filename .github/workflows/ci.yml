name: 🚀 Node.js CI with Docker

on:
  push:
    branches:
    - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: test echo
      run: echo "Hello, World!""


